# Seed Research

No CVE, GHSA, advisory id, package-version seed, or named external vulnerability was provided by the user. npm audit was attempted locally but blocked because it would send dependency metadata to the external npm registry without explicit user approval. No advisory-seeded rows were opened.
